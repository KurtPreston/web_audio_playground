{
  "title": "Microphone Audio Settings",
  "type": "object",
  "properties": {
    "maxAudibleDistance": {
      "title": "Max Audible Distance",
      "type": "number",
      "minimum": 100,
      "maximum": 5000
    },
    "distanceVolumeRolloff": {
      "title": "Distance-Volume Roll-off",
      "type": "number",
      "minimum": 1,
      "maximum": 20
    },
    "maxNodeVolume": {
      "title": "Max Node Volume (db)",
      "type": "number",
      "minimum": -30,
      "maximum": 0
    },
    "dopplerMode": {
      "title": "Doppler Mode",
      "type": "string",
      "enum": ["Off", "On", "Invert"],
      "enumNames": ["Off", "On", "Invert"],
      "tsEnumNames": ["Off", "On", "Invert"],
      "default": "Off"
    },
    "speedOfSound": {
      "title": "Speed of Sound",
      "type": "number",
      "minimum": 1,
      "maximum": 5000
    }
  },
  "required": [
    "dopplerMode",
    "speedOfSound",
    "distanceVolumeRolloff",
    "maxAudibleDistance",
    "maxNodeVolume"
  ],
  "additionalProperties": false
}

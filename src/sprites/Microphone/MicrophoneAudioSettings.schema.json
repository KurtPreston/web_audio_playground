{
  "title": "Microphone Audio Settings",
  "type": "object",
  "properties": {
    "maxAudibleDistance": {
      "title": "Max Audible Distance",
      "type": "number",
      "minimum": 100,
      "maximum": 5000
    },
    "distanceVolumeRolloff": {
      "title": "Distance-Volume Roll-off",
      "type": "number",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    },
    "minVolume": {
      "title": "Min Volume (dB)",
      "type": "number",
      "minimum": -100,
      "maximum": 0,
      "default": -35
    },
    "maxVolume": {
      "title": "Max Volune (dB)",
      "type": "number",
      "minimum": -100,
      "maximum": 0,
      "default": -4
    },
    "dopplerMode": {
      "title": "Doppler Mode",
      "type": "string",
      "enum": ["Off", "On", "Invert"],
      "enumNames": ["Off", "On", "Invert"],
      "tsEnumNames": ["Off", "On", "Invert"],
      "default": "Off"
    },
    "speedOfSound": {
      "title": "Speed of Sound",
      "type": "number",
      "minimum": 1,
      "maximum": 5000
    }
  },
  "required": [
    "dopplerMode",
    "speedOfSound",
    "distanceVolumeRolloff",
    "maxAudibleDistance",
    "minVolume",
    "maxVolume"
  ],
  "additionalProperties": false
}

{
  "title": "Midi File Options",
  "type": "object",
  "properties": {
    "bpm": {
      "type": "number",
      "title": "BPM",
      "minimum": 50,
      "maximum": 200
    },
    "midiFileUri": {
      "type": "string",
      "format": "uri-reference",
      "title": "Midi File",
      "enum": ["/midi/moonlight_sonata.mid", "/midi/scarborough.mid"]
    }
  },
  "required": ["bpm"],
  "additionalProperties": false
}

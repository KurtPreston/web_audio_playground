{
  "title": "Doppler Settings",
  "type": "object",
  "properties": {
    "mode": {
      "title": "Doppler Mode",
      "type": "integer",
      "enum": ["Off", "On", "Invert"],
      "enumNames": ["Off", "On", "Invert"],
      "tsEnumNames": ["Off", "On", "Invert"],
      "default": "Off"
    },
    "speedOfSound": {
      "title": "Speed of Sound",
      "type": "number",
      "minimum": 1,
      "maximum": 5000
    }
  },
  "required": ["mode", "speedOfSound"],
  "additionalProperties": false
}
